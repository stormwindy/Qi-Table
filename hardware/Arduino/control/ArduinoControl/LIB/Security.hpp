#ifndef SECURITY_H //include guard
#define SECURITY_H

bool setUpEncodedDirections(char handshake[]);

char convertToDirection(char toBeConverted);

char getDirection(char encodedDirection);


#endif
